GOOGLE_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-3.6-flash
EMBEDDING_MODEL=models/text-embedding-004
PORT=8080
# Set to 1 to serve canned fixtures instead of calling Gemini.
# Leaving GOOGLE_API_KEY unset does the same thing automatically.
LINECASE_OFFLINE=0
